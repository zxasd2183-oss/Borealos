{
  "name": "@borealos/database",
  "version": "0.1.0",
  "private": true,
  "description": "BorealOS 数据库抽象层 - 提供统一的数据库适配器接口，支持内存、PostgreSQL 和 Redis 缓存",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
