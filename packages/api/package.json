{
  "name": "@borealos/api",
  "version": "0.1.0",
  "private": true,
  "description": "BorealOS API SDK 客户端 - 封装 HTTP 请求和 WebSocket 通信",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
