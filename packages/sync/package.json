{
  "name": "@borealos/sync",
  "version": "0.1.0",
  "private": true,
  "description": "BorealOS 实时同步包 - 基于 Yjs CRDT 的多平台实时同步与协作编辑",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
