const assert = require("node:assert/strict");
const fs = require("node:fs");
const source = fs.readFileSync("app/src/main/java/com/nexa/app/MainActivity.java", "utf8");
const manifest = fs.readFileSync("app/src/main/AndroidManifest.xml", "utf8");
assert.match(source, /BorealosFeedbackBridge/);
assert.match(source, /captureCurrentActivity/);
assert.match(source, /Bitmap\.CompressFormat\.PNG/);
assert.match(source, /FLAG_SECURE/);
assert.match(source, /isTrustedBorealosUrl/);
assert.match(source, /webView\.getUrl\(\)/);
assert.match(source, /request\.getUrl\(\)/);
assert.match(source, /"borealos\.dev"\.equalsIgnoreCase\(host\)/);
assert.doesNotMatch(source, /host\.(?:contains|startsWith)\("borealos\.dev"\)/);
assert.match(source, /port == -1 \|\| port == 443/);
assert.match(source, /port == 18790/);
assert.match(source, /runOnUiThread\(\(\) -> \{[\s\S]*feedbackForeground[\s\S]*webView\.getUrl\(\)[\s\S]*FLAG_SECURE[\s\S]*webView\.draw/);
assert.match(source, /JavascriptInterface/);
assert.doesNotMatch(manifest, /READ_MEDIA_IMAGES|WRITE_EXTERNAL_STORAGE|READ_EXTERNAL_STORAGE/);
console.log("android feedback bridge checks passed");
