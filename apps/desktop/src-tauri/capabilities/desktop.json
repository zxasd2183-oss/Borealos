{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Aurora 桌面端独有权限（窗口状态持久化）",
  "windows": ["login", "main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "window-state:default"
  ]
}
